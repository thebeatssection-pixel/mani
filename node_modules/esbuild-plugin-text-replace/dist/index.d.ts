import 'ts-replace-all';
type replacePattern = [RegExp | string, string | any];
export interface optionI {
    include?: RegExp;
    namespace?: string;
    pattern: replacePattern[];
}
interface pipeI {
    transform?: null | {
        contents: string;
        args: {
            path: string;
            namespace: string;
            suffix: string;
            pluginData: any;
        };
    };
}
declare function textReplace(options?: optionI): {
    name: string;
    setup(build: any, { transform }?: pipeI): {
        contents: string;
    };
};
export default textReplace;
